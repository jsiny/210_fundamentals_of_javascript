for (let i = 0; i < 5; ) {
  console.log(i += 1);
}

// This code outputs 1, 2, 3, 4, 5.
// The 3 components of the for loop are optional. In this code, the 'next value' component is omitted but this isn't a problem as the loop variable is incremented within the loop.
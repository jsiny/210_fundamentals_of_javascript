var apples = 3;
var bananas = '3';

if (apples === bananas) {
  console.log('Apples are equal to Bananas');
}
 
// This version does not output a message because the === operator compares the values and the type. A string and a number can't be strictly equal.